# Shared

Ce dossier contient le code commun aux features.

```txt
src/shared/
└─ styles/
   └─ globals.css
```

Ajoute ici uniquement ce qui peut vraiment etre reutilise par plusieurs parties du projet.
